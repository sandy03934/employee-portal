import { SearchEmployeeResponse } from './search-employee-response';

describe('SearchEmployeeResponse', () => {
  it('should create an instance', () => {
    expect(new SearchEmployeeResponse()).toBeTruthy();
  });
});
